name: Security Checks

on:
  push:
  pull_request:

jobs:
  bidi-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check bidi hidden unicode
        run: python3 tool/security/check_bidi.py
